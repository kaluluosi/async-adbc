# V2.0 第二轮开发计划

## 概览

第一轮已完成：模块拆分、插件注册表、兼容层。

第二轮包含：uv 迁移、测试重构、文档完善、连接复用、插件优化、API 统一。

---

## 阶段一：uv 迁移

### 目标
从 poetry 完全迁移到 uv，使用 Python 3.12

### 任务清单
- [ ] 删除 `poetry.lock`
- [ ] 重写 `pyproject.toml`：
  - [ ] 移除 `[tool.poetry]` 配置
  - [ ] 使用标准 `[project]` 配置
  - [ ] 更新 `[build-system]`
  - [ ] 添加 `[project.optional-dependencies]`：
    - `test`: pytest, pytest-asyncio, pytest-mock, pydocstyle, coverage
    - `docs`: mkdocs, mkdocs-material
- [ ] 用 `uv sync` 创建虚拟环境并验证
- [ ] 更新 `.gitignore` 添加 uv 相关
- [ ] 提交

---

## 阶段二：测试框架重构

### 目标
删除旧 tests 完全重写为 pytest + mock

### 任务清单
- [ ] 删除整个旧 `tests/` 目录
- [ ] 创建新测试目录结构：
  ```
  tests/
  ├── conftest.py
  ├── unit/
  ├── service/
  └── plugins/
  ```
- [ ] 实现核心 fixtures：
  - [ ] `mock_connection_factory`
  - [ ] `mock_adbclient`
  - [ ] `mock_device`
  - [ ] `mock_shell_output`
- [ ] 编写 unit 测试：
  - [ ] test_models.py
  - [ ] test_protocol.py
  - [ ] test_utils.py
- [ ] 编写 service 测试：
  - [ ] test_host.py
  - [ ] test_local.py
- [ ] 编写 plugins 测试：
  - [ ] test_pm.py
  - [ ] test_cpu.py
  - [ ] test_mem.py
  - [ ] test_temp.py
  - [ ] 其他插件...
- [ ] 运行测试验证
- [ ] 提交

---

## 阶段三：连接复用优化

### 目标
实现持久连接管理

### 任务清单
- [ ] 修改 `service/base.py`：
  - [ ] 添加 `_conn: Optional[Connection]`
  - [ ] 添加 `_lock: asyncio.Lock`
  - [ ] 实现 `_get_connection()`
  - [ ] 实现 `_close_connection()`
  - [ ] 修改 `request()` / `request_without_check()` 复用连接
- [ ] 修改 `ADBClient` 添加 `close()` 方法
- [ ] 修改 `Device` 添加 `close()` 方法
- [ ] 确保 push/pull 仍用独立连接
- [ ] 实现自动重连
- [ ] 运行测试验证
- [ ] 提交

---

## 阶段四：API 风格统一

### 目标
移除 async property，统一为 async 方法

### 任务清单
- [ ] 修改 `device.py`：
  - [ ] `properties` → `get_properties()`
  - [ ] `get_pid_by_pkgname()` 已是方法
  - [ ] `file_exists()` 已是方法
- [ ] 修改 `plugins/cpu.py`：
  - [ ] `count` → `get_count()`
  - [ ] `freqs` → `get_freqs()`
  - [ ] `normalize_factor` → `get_normalize_factor()`
  - [ ] `cpu_stats` → `get_cpu_stats()`
  - [ ] `cpu_usages` → `get_cpu_usages()`
  - [ ] `total_cpu_stat` → `get_total_cpu_stat()`
  - [ ] `total_cpu_usage` → `get_total_cpu_usage()`
  - [ ] `cpu_name` → `get_cpu_name()`
  - [ ] `info` → `get_info()`
- [ ] 修改 `plugins/mem.py`：
  - [ ] `info` → `get_info()`
- [ ] 修改 `plugins/gpu.py`：
  - [ ] 检查并修改
- [ ] 修改 `plugins/prop.py`：
  - [ ] 检查并修改
- [ ] 其他插件检查
- [ ] 添加兼容层（可选，如需保留旧 API 别名
- [ ] 运行测试验证
- [ ] 提交

---

## 阶段五：插件兼容性优化

### 目标
添加多层降级和 `check=False` 参数

### 任务清单
- [ ] 实现工具函数 `_try_shell_commands()`
- [ ] 修改 `plugins/cpu.py`：
  - [ ] `get_count()` 添加备用方案
  - [ ] `get_freqs()` 添加备用方案
  - [ ] `get_cpu_stats()` 添加备用方案
  - [ ] 所有方法添加 `check=False` 参数
- [ ] 修改 `plugins/mem.py`：
  - [ ] `get_info()` 添加备用方案
  - [ ] `stat()` 添加备用方案
  - [ ] 添加 `check=False` 参数
- [ ] 修改 `plugins/temp.py`：
  - [ ] `stat()` 添加备用方案
  - [ ] 添加 `check=False` 参数
- [ ] 其他插件检查并添加容错
- [ ] 运行测试验证
- [ ] 提交

---

## 阶段六：文档注释完善

### 目标
完善所有公共 API 的 docstring

### 任务清单
- [ ] 完善 `device.py`
- [ ] 完善 `service/host.py`
- [ ] 完善 `service/local.py`
- [ ] 完善 `plugins/pm.py`
- [ ] 完善 `plugins/cpu.py`
- [ ] 完善 `plugins/mem.py`
- [ ] 完善其他插件
- [ ] 完善 `protocol/` 模块
- [ ] 完善 `models.py`
- [ ] 完善 `exceptions.py`
- [ ] 运行 pydocstyle 检查
- [ ] 提交

---

## 验证清单

- [ ] 所有 pytest 测试通过
- [ ] 手动验证基本功能（连接设备、shell、push/pull）
- [ ] pydocstyle 检查通过
- [ ] GitHub workflows 更新（可选，本次暂不改动）

---

## 里程碑

- 每个阶段完成后提交 git
